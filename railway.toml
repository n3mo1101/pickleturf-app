[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
preDeployCommand = "python manage.py migrate && python manage.py seed_courts"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"