[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
preDeployCommand = "/opt/venv/bin/python manage.py migrate && /opt/venv/bin/python manage.py seed_courts"
restartPolicyType = "on_failure"

